                <div className="data-array-wrapper">
                {data.map((member, index) => (
                    <form
                        key={`member${member.id}`}
                        name={`memberForm-${index}`}
                        id={index}
                        className={`member-form-${index} member-formMID-${member.id} member-forms`}
                    >
                        {/* Member form fields before toggling info visible  */}
                        <section key={`sectionVisible${member.id}`} className="member-section-visible">
                        <p key={`sectionp${member.id}`} className="id-value">{index}.</p>

                        <input
                           key={`firstName${member.id}`}
                            type="text"
                            name="firstName"
                            id={member.id}
                            className="input-firstName"
                            value={data[index].firstName}
                            onChange={handleChange}
                        />

                        <input
                        key={`lastName${member.id}`}
                            type="text"
                            name="lastName"
                            id={member.id}
                            className="input-lastName"
                            value={data[index].lastName}
                            onChange={handleChange}
                        />

                        <input
                        key={`ffposition${member.id}`}
                            type="text"
                            name="ffposition"
                            id={member.id}
                            className="input-position"
                            value={data[index].ffposition}
                            onChange={handleChange}
                        />

                        <img
                        key={`firetruck${member.id}`}
                            src="https://drive.google.com/uc?export=download&id=15q0SsoW8GseByceDXJxG7UaclNQYBVJY"
                            className={`fireTruck fireTruck-${index}`}
                            style={{ display: "none" }}
                            alt='Fire truck'
                        />

                        <img
                        key={`memberInfoBtn${member.id}`}
                            src="https://drive.google.com/uc?export=download&id=1u2Eib4hTRffN1aaTLscKze-L6dLN0RKl"
                            name="memberInfoBtn"
                            id={`memberInfoBtn-${index}`}
                            className="memberInfoBtn"
                            alt="Arrow"
                            title="Info aufklappen"
                            onClick={() => {
                            toggleMemberInfo(index);
                            }}
                        />
                        </section>

                        {/* Member info Form fields */}
                        <section
                        key={`section2${member.id}`}
                        id={`infoSection-${index}`}
                        className="member-section-toToggle"
                        style={{ display: "none" }}
                        >
                        <div className="memberInfo-wrapper">
                        key={`memberInfoWarpper${member.id}`}
                            <h3 className="memberInfos-h3">Infos</h3>
                            <textarea
                            key={`textarea${member.id}`}
                            name="textarea"
                            id={member.id}
                            className="input-textarea"
                            value={data[index].textarea}
                            onChange={handleChange}
                            />

                            <img
                            key={`distinguihser${member.id}`}
                            src="https://drive.google.com/uc?export=download&id=1L1sxkr7IVMtQEHTjFgWudgX7jWbLJ8-c"
                            className="memberInfo-fireImage"
                            alt="Fire distinguisher"
                            />

                            <div  key={`manipulate${member.id}`}className="firestore-manipulateMember-btn">
                            <input
                            key={`delete${member.id}`}
                                type="button"
                                name="deleteBtn"
                                id={member.id}
                                title="Kamerad:in unwiderruflich aus der Datenbank löschen"
                                className={`deleteBtn delete-${member.id}`}
                                onClick={(event) => {
                                handleDeleteFirestoreMember(member.id);
                                }}
                                defaultValue="löschen"
                            />

                            <input
                            key={`update${member.id}`}
                                type="button"
                                name="updateBtn"
                                id={member.id}
                                title="Update die Daten der/des Kamerad:in in der Datenbank"
                                className={`updateBtn update-${member.id}`}
                                onClick={(event) => {
                                handleUpdateFirestoreMember(member.id);
                                }}
                                defaultValue="update"
                            />
                            </div>
                        </div>

                        <div key={`gala${member.id}`} className="uniformenTypes-div">
                            <h3 key={`galah3${member.id}`}>Galauniformen</h3>

                            <div key={`galalbaleDiv${member.id}`} className="galaLabel-div uniformenLabels-div">
                            <p key={`galap1${member.id}`} className="galaP1">Größe</p>
                            <p key={`galap2${member.id}`} className="galaP2" title="Bereits ausgegeben?">
                                aus?
                            </p>
                            </div>

                            <div key={`galaAseet${member.id}`} className="mantel-div uniformenAssets-divs galaAsset">
                            <p key={`mantelp${member.id}`}>Mantel</p>

                            <input
                            key={`mantels${member.id}`}
                                type="text"
                                name="mantelS"
                                id={member.id}
                                value={data[index].mantelS}
                                onChange={handleChange}
                            />

                            <input
                            key={`mb${member.id}`}
                                type="checkbox"
                                name="mantelB"
                                id={member.id}
                                checked={data[index].mantelB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div  key={`j${member.id}`}className="jacke-div uniformenAssets-divs galaAsset">
                            <p key={`jp${member.id}`}>Jacke</p>

                            <input
                            key={`j${member.id}`}
                                type="text"
                                name="jackeS"
                                id={member.id}
                                value={data[index].jackeS}
                                onChange={handleChange}
                            />

                            <input
                            key={`jb${member.id}`}
                                type="checkbox"
                                name="jackeB"
                                id={member.id}
                                checked={data[index].jackeB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`h${member.id}`} className="hose-rock-div uniformenAssets-divs galaAsset">
                            <p key={`hp${member.id}`}>Hose</p>

                            <input
                            key={`hs${member.id}`}
                                type="text"
                                name="hoseS"
                                id={member.id}
                                value={data[index].hoseS}
                                onChange={handleChange}
                            />

                            <input
                            key={`hb${member.id}`}
                                type="checkbox"
                                name="hoseB"
                                id={member.id}
                                checked={data[index].hoseB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`he${member.id}`} className="hemd-div uniformenAssets-divs galaAsset">
                            <p key={`hep${member.id}`}>Hemd</p>

                            <input
                            key={`hes${member.id}`}
                                type="text"
                                name="hemdS"
                                id={member.id}
                                value={data[index].hemdS}
                                onChange={handleChange}
                            />

                            <input
                            key={`heb${member.id}`}
                                type="checkbox"
                                name="hemdB"
                                id={member.id}
                                checked={data[index].hemdB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`k${member.id}`} className="kappe-div uniformenAssets-divs galaAsset">
                            <p key={`kp${member.id}`}>Kappe</p>

                            <input
                            key={`ks${member.id}`}
                                type="text"
                                name="kappeS"
                                id={member.id}
                                value={data[index].kappeS}
                                onChange={handleChange}
                            />

                            <input
                            key={`kb${member.id}`}
                                type="checkbox"
                                name="kappeB"
                                id={member.id}
                                checked={data[index].kappeB || false}
                                onChange={handleChange}
                            />
                            </div>
                        </div>

                        <div  key={`uni2${member.id}`} className="uniformenTypes-div">
                            <h3 key={`uni2h3${member.id}`}>Dienstbekleidung</h3>

                            <div key={`dienstDiv${member.id}`} className="dienstbekleidungLabel-div uniformenLabels-div">
                            <p key={`dienstP1${member.id}`} className="uLabelsP1">Größe</p>
                            <p key={`dienstP2${member.id}`} className="uLabelsP2" title="Bereits ausgegeben?">
                                aus?
                            </p>
                            </div>

                            <div key={`pull${member.id}`} className="pullover-div uniformenAssets-divs dienstAsset">
                            <p key={`pullP${member.id}`} >Pullover</p>

                            <input
                            key={`pullS${member.id}`}
                                type="text"
                                name="pulloverS"
                                id={member.id}
                                value={data[index].pulloverS}
                                onChange={handleChange}
                            />

                            <input
                            key={`pullB${member.id}`}
                                type="checkbox"
                                name="pulloverB"
                                id={member.id}
                                checked={data[index].pulloverB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`hose2Div${member.id}`} className="hose2-div uniformenAssets-divs dienstAsset">
                            <p key={`hose2P${member.id}`}>Hose</p>

                            <input
                            key={`hose2S${member.id}`}
                                type="text"
                                name="hose2S"
                                id={member.id}
                                value={data[index].hose2S}
                                onChange={handleChange}
                            />

                            <input
                            key={`hose2B${member.id}`}
                                type="checkbox"
                                name="hose2B"
                                id={member.id}
                                checked={data[index].hose2B || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`ts${member.id}`} className="tshirt-div uniformenAssets-divs dienstAsset">
                            <p key={`tsP${member.id}`}>T-Shirt</p>

                            <input
                            key={`tss${member.id}`}
                                type="text"
                                name="tshirtS"
                                id={member.id}
                                value={data[index].tshirtS}
                                onChange={handleChange}
                            />

                            <input
                           key={`tsb${member.id}`} 
                                type="checkbox"
                                name="tshirtB"
                                id={member.id}
                                checked={data[index].tshirtB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`po${member.id}`} className="polo-div uniformenAssets-divs dienstAsset">
                            <p key={`poP${member.id}`}>Polo</p>

                            <input
                            key={`poS${member.id}`}
                                type="text"
                                name="poloS"
                                id={member.id}
                                value={data[index].poloS}
                                onChange={handleChange}
                            />

                            <input
                            key={`poB${member.id}`}
                                type="checkbox"
                                name="poloB"
                                id={member.id}
                                checked={data[index].poloB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`bl${member.id}`} className="Bluse-div uniformenAssets-divs dienstAsset">
                            <p key={`blP${member.id}`}>Bluse</p>

                            <input
                            key={`blS${member.id}`}
                                type="text"
                                name="bluseS"
                                id={member.id}
                                value={data[index].bluseS}
                                onChange={handleChange}
                            />

                            <input
                            key={`blB${member.id}`}
                                type="checkbox"
                                name="bluseB"
                                id={member.id}
                                checked={data[index].bluseB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`fl${member.id}`} className="fleece-div uniformenAssets-divs dienstAsset">
                            <p key={`flP${member.id}`}>Fleece</p>

                            <input
                            key={`flS${member.id}`}
                                type="text"
                                name="fleeceS"
                                id={member.id}
                                value={data[index].fleeceS}
                                onChange={handleChange}
                            />

                            <input
                            key={`flB${member.id}`}
                                type="checkbox"
                                name="fleeceB"
                                id={member.id}
                                checked={data[index].fleeceB || false}
                                onChange={handleChange}
                            />
                            </div>
                        </div>

                        <div key={`uni3${member.id}`} className="uniformenTypes-div">
                            <h3 key={`uni3H3${member.id}`}>Einsatzuniform</h3>

                            <div key={`ei${member.id}`} className="einsatzuniformenLabel-div uniformenLabels-div">
                            <p key={`eiP1${member.id}`} className="uLabelsP1">Größe</p>
                            <p key={`eiP2${member.id}`} className="uLabelsP2" title="Bereits ausgegeben?">
                                aus?
                            </p>
                            </div>

                            <div key={`schJ${member.id}`} className="schutzjacke-div uniformenAssets-divs einsatzAsset">
                            <p key={`schJP${member.id}`}>Schutzjacke</p>

                            <input
                            key={`schJS${member.id}`}
                                type="text"
                                name="schutzjackeS"
                                id={member.id}
                                value={data[index].schutzjackeS}
                                onChange={handleChange}
                            />

                            <input
                            key={`schJB${member.id}`}
                                type="checkbox"
                                name="schutzjackeB"
                                id={member.id}
                                checked={data[index].schutzjackeB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`schH${member.id}`} className="schutzhose-div uniformenAssets-divs einsatzAsset">
                            <p key={`schHP${member.id}`} >Schutzhose</p>

                            <input
                            key={`schHS${member.id}`}
                                type="text"
                                name="schutzhoseS"
                                id={member.id}
                                value={data[index].schutzhoseS}
                                onChange={handleChange}
                            />

                            <input
                            key={`schHB${member.id}`}
                                type="checkbox"
                                name="schutzhoseB"
                                id={member.id}
                                checked={data[index].schutzhoseB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`ess${member.id}`} className="einsatzstiefelschwarz-div uniformenAssets-divs einsatzAsset">
                            <p key={`essP${member.id}`}>Einsatzstiefel schwarz</p>

                            <input
                            key={`essS${member.id}`}
                                type="text"
                                name="einsatzstiefelschwarzS"
                                id={member.id}
                                value={data[index].einsatzstiefelschwarzS}
                                onChange={handleChange}
                            />

                            <input
                            key={`essB${member.id}`}
                                type="checkbox"
                                name="einsatzstiefelschwarzB"
                                id={member.id}
                                checked={data[index].einsatzstiefelschwarzB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`essG${member.id}`} className="einsatzstiefelgelb-div uniformenAssets-divs einsatzAsset">
                            <p key={`essGP${member.id}`} >Einsatzstiefel gelb</p>

                            <input
                            key={`essGS${member.id}`} 
                                type="text"
                                name="einsatzstiefelgelbS"
                                id={member.id}
                                value={data[index].einsatzstiefelgelbS}
                                onChange={handleChange}
                            />

                            <input
                            key={`essGB${member.id}`} 
                                type="checkbox"
                                name="einsatzstiefelgelbB"
                                id={member.id}
                                checked={data[index].einsatzstiefelgelbB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`esHS${member.id}`}  className="einsatzhandschuhe-div uniformenAssets-divs einsatzAsset">
                            <p key={`esHSP${member.id}`}>Einsatzhandschuhe</p>

                            <input
                            key={`esHSS${member.id}`}
                                type="text"
                                name="einsatzhandschuheS"
                                id={member.id}
                                value={data[index].einsatzhandschuheS}
                                onChange={handleChange}
                            />

                            <input
                            key={`esHSB${member.id}`}
                                type="checkbox"
                                name="einsatzhandschuheB"
                                id={member.id}
                                checked={data[index].einsatzhandschuheB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`esST${member.id}`} className="einsatzstiefel-div uniformenAssets-divs einsatzAsset">
                            <p key={`esSTP${member.id}`} >Einsatzstiefel</p>

                            <input
                            key={`esSTS${member.id}`} 
                                type="text"
                                name="einsatzstiefelS"
                                id={member.id}
                                value={data[index].einsatzstiefelS}
                                onChange={handleChange}
                            />

                            <input
                            key={`esSTB${member.id}`} 
                                type="checkbox"
                                name="einsatzstiefelB"
                                id={member.id}
                                checked={data[index].einsatzstiefelB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`kappe3${member.id}`}  className="kappe3-div uniformenAssets-divs einsatzAsset">
                            <p key={`kappe3P${member.id}`}>Kappe</p>

                            <input
                            key={`kappe3S${member.id}`}
                                type="text"
                                name="kappe3S"
                                id={member.id}
                                value={data[index].kappe3S}
                                onChange={handleChange}
                            />

                            <input
                            key={`kappe3B${member.id}`}
                                type="checkbox"
                                name="kappe3B"
                                id={member.id}
                                checked={data[index].kappe3B || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`haub${member.id}`} className="haube-div uniformenAssets-divs einsatzAsset">
                            <p  key={`haubP${member.id}`}>haube</p>

                            <input
                             key={`haubS${member.id}`}
                                type="text"
                                name="haubeS"
                                id={member.id}
                                value={data[index].haubeS}
                                onChange={handleChange}
                            />

                            <input
                             key={`haubB${member.id}`}
                                type="checkbox"
                                name="haubeB"
                                id={member.id}
                                checked={data[index].haubeB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div  key={`helm${member.id}`} className="helm-div uniformenAssets-divs einsatzAsset">
                            <p key={`helmP${member.id}`} >Helm</p>

                            <input
                            key={`helmS${member.id}`} 
                                type="text"
                                name="helmS"
                                id={member.id}
                                value={data[index].helmS}
                                onChange={handleChange}
                            />

                            <input
                            key={`helmB${member.id}`} 
                                type="checkbox"
                                name="helmB"
                                id={member.id}
                                checked={data[index].helmB || false}
                                onChange={handleChange}
                            />
                            </div>

                            <div key={`gurt${member.id}`}  className="Gurt-div uniformenAssets-divs einsatzAsset">
                            <p key={`gurtP${member.id}`}>Gurt</p>

                            <input
                            key={`gurtS${member.id}`}
                                type="text"
                                name="GurtS"
                                id={member.id}
                                value={data[index].GurtS}
                                onChange={handleChange}
                            />

                            <input
                            key={`gurtB${member.id}`}
                                type="checkbox"
                                name="GurtB"
                                id={member.id}
                                checked={data[index].GurtB || false}
                                onChange={handleChange}
                            />
                            </div>
                        </div>
                        </section>

                    </form>
                    ))}